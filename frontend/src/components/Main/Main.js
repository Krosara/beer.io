import React from 'react';

const Main = ({ data }) => {
  return (
    <div>
      {/* {data.map((player, i) => (
        <h1 key={i}>{player.username}</h1>
      ))} */}
    </div>
  );
};

export default Main;
