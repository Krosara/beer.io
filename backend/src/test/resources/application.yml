spring:
  datasource:
    password: sa
    url: h2:postgresql://mem:db;DB_CLOSE_DELAY=-1
    username: sa
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop